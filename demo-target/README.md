This directory holds a small sample codebase that later agents can analyze.

Nothing in demo-target has been scanned or scored. It exists so the command center
has a local path you can register during development.
